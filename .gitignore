__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
/build/
/dist/
*.egg-info/
.DS_Store

# rust
target/
.gstack/

# generated dev snapshot
gui/dist/snapshot.json

# tauri build output
gui/src-tauri/target/
gui/src-tauri/gen/schemas/

# updater signing keys. The private half lives in ~/.tauri and in a repository
# secret, never here — see RELEASING.md. This is here so a pair generated into
# the working tree by accident cannot be committed.
*.key
*.key.pub
